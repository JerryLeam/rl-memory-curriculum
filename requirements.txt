# Core ML
torch>=2.1.0
transformers>=4.48.0
accelerate>=1.2.0
datasets>=3.2.0

# GRPO training (TRL + LoRA)
trl>=0.15.0
peft>=0.14.0

# Retrieval (embedding-based memory search)
sentence-transformers>=3.0.0

# Evaluation
numpy>=1.24.0
pandas>=2.0.0
nltk>=3.8.0
scikit-learn>=1.3.0

# Config
pyyaml

# LLM-as-Judge (OpenAI-compatible API)
openai>=1.0.0


# Optional: faster attention on GPU (install separately)
# pip install flash-attn --no-build-isolation
