# Config B (Full FT): Mixed curriculum (LoCoMo + LongMemEval)
# Requires H100 80GB or A100 80GB
experiment:
  name: "config_b_mixed"
  seed: 42

model:
  name: "Qwen/Qwen2.5-7B-Instruct"

data:
  train_file: "data/processed/mixed_train.jsonl"
  val_file: "data/processed/mixed_val.jsonl"

training:
  algorithm: "grpo"
  use_lora: false
  gradient_checkpointing: false
  batch_size: 4
  group_size: 4
  learning_rate: 1.0e-6
  gradient_accumulation_steps: 2
  train_answer_agent: true
  train_memory_manager: true
  aa_epochs: 2
  mm_epochs: 2
  max_seq_length: 4096
  aa_max_completion_length: 1024
  mm_max_completion_length: 256

reward:
  type: "f1"

retrieval:
  top_k: 60

wandb:
  project: "rl-memory-curriculum"
  entity: null
  enabled: false
